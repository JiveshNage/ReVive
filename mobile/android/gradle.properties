org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Disable incremental compilation to prevent cross-drive (C: vs D:) path root mismatch on Windows
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false

# Use isolated Gradle user home on D: drive to prevent lock contention with other IDE daemons
systemProp.gradle.user.home=D:/ReVive/.gradle_build

